<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".MountainActivity">

    <!-- Toolbar -->
    <include layout="@layout/toolbar"/>

    <!-- Photo of mountain -->
    <ImageView
        android:id="@+id/mountainPhoto"
        android:layout_marginTop="80dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_height="310dp"
        android:layout_width="match_parent"
        app:srcCompat="@drawable/temp_diablerets"
        android:contentDescription="@string/description_collected_image" />

    <!-- Info text container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/mountainPhoto"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <!-- Height -->
        <TextView
            android:id="@+id/heightText"
            style="@style/LargeDarkGreenText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Height:  3,210m" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>